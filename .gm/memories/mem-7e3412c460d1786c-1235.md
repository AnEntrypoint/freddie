---
key: mem-7e3412c460d1786c-1235
ns: default
created: 1779811334078
updated: 1779811334078
---

Freddie substrate gotchas batch 2: (1) codeinsight detector limits — regex-only. "hardcoded secrets" and "SQL injection" flags are pure regex matches on identifier substrings, not value or AST analysis. Confirmed FPs: env-var names like DAYTONA_API_KEY in process.env.X; function param names like 'secret' in HMAC helpers; URL query keys like ?appkey=&appsecret=; error-message string literals containing _API_KEY; HTTP DELETE URL paths flagged as SQL DELETE FROM. Recurrence tell: if flagged line is process.env.X, fetch URL, or function param, almost certainly FP. (2) codeinsight orphan detector blind spots — misses three reachability paths: dynamic await import('./path/' + variable + '.js') strings; plugin auto-discovery walking plugins/<dir>/plugin.js from discoverPlugins(); HTTP-served static files like src/web/app.js referenced only from index.html. 2026-05-12 cleanup: 19 dead files deleted. (3) createHost decomposition (2026-05-12): src/host/host.js was 197L with 111L body. Refactored: helper factories (makePi, makeGui, makeCcHooks, makeHooksRegistry, makeCcLoaders + reg, guard, scopedCfg, nullStore) extracted to src/host/host_helpers.js (152L). host.js shrinks to 64L; createHost body ~24L. Both under 200L cap.
