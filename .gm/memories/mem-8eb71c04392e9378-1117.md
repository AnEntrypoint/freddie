---
key: mem-8eb71c04392e9378-1117
ns: default
created: 1779811334076
updated: 1779811334076
---

Freddie substrate gotchas batch 1: (1) Browser inline <script type="module"> syntax errors — when pageerror reports "missing ) after argument list" with no file:line, extract script to .js file and run "node --check path/to/file.js" for exact line; browsers swallow line numbers for inline modules. (2) src/web/app.js 200-line policy violation — file is 548 lines (2.7x over gm hard cap); only file in 283-file codebase over limit. When touching app.js, prefer splitting into {app,routes,components,state}.js. (3) libsql async debt: src/sessions.js (listSessions/search/getMessages/createSession/appendMessage) and src/cron/scheduler.js (listJobs/createJob/cancelJob/deleteJob) are async after libsql migration. Sync callsites silently wrap each call in Promise rejecting on iteration, surfacing as TypeError "... is not iterable". Rule: every call must be awaited. test.js can pass while CLI broken — exercise cli verb in test.js or smoke "node bin/freddie.js <verb>". (4) Bulk-rename: git grep is case-sensitive on literal patterns. For case-variant sweep use "git grep -liI -e <lower> -e <Title> -e <UPPER>".
