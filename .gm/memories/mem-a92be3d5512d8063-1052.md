---
key: mem-a92be3d5512d8063-1052
ns: default
created: 1783446947343
updated: 1783446947343
---

## Resolved mutable: acptoapi-daemon-no-keys

Root cause: freddie had zero dotenv wiring (grep confirmed no dotenv require/import in src/bin/plugins). Fixed: (1) copied freddie/.env into ../acptoapi/.env so standalone daemon's existing dotenv.config() picks up real keys; (2) added dotenv.config() at top of src/host/index.js (every entrypoint imports bootHost from here before touching provider keys) + added dotenv as direct package.json dependency. Live witness: node -e importing acptoapi-bridge.js::callLLM with model groq/llama-3.3-70b-versatile returned real content {content:'Hello is nice.'} and, with tool_choice:required, correct tool_calls:[{name:'write_file',arguments:{path:'hello.txt',content:'Hello World'}}] -- previously returned {error:invalid_api_key}. Also fixed a real acptoapi bug found along the way: lib/model-probe-live.js:75 sent literal model:'test' to brand provider chat endpoints causing false 404 probe-FAILED even with valid keys; fixed to use KNOWN[provider][0], verified groq API 404->200 with the corrected model id.
