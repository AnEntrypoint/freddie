- id: dashboard-models-page-polish
  subject: Polish #/models dashboard — drag-reorder + sampler tile + window.__debug.models
  status: pending
  description: Existing models page (in anentrypoint-design pages-config.js export `models`) lists providers + probe. Missing — drag-reorder fallback list persisting to agent.model_preference, sampler status tile reading /api/models/sampler, window.__debug.models() returning {providers, queues, ranking, sampler}. Implementation lives in C:/dev/anentrypoint-design/src — freddie pins file:../anentrypoint-design so changes flow through after rebuild (node scripts/build.mjs in ADP).
  effort: large
  category: feature
  route_family: observability
  load: 0.7
  failure_modes: [observability/no-debug-surface]
  route_fit: examined
  authorization: weak_prior
  blocking: []
  blockedBy: []
  acceptance:
    - models page renders drag-reorder list; reorder persists to agent.model_preference
    - sampler status tile reads /api/models/sampler and updates on probe failure
    - window.__debug.models() returns {providers, queues, ranking, sampler}

- id: dynamic-model-picker-everywhere
  subject: Replace every manual model text input with a dropdown sourced from /v1/models
  status: pending
  description: Audit every model input in dashboard (chat composer in ADP pages-chat.js line ~187 has h('input',{name:'model',type:'text'...}); config page agent.model input). Replace with select dropdown grouped by provider, fetched from /v1/models on mount, filtered by sampler.isAvailable. Lives in anentrypoint-design src — rebuild dist after edits.
  effort: large
  category: refactor
  route_family: representation
  load: 0.8
  failure_modes: [representation/manual-entry-trap]
  route_fit: examined
  authorization: weak_prior
  blocking: []
  blockedBy: []
  acceptance:
    - chat composer model field is a dropdown grouped by provider, sourced from /v1/models
    - config page agent.model field is a dropdown
    - window.__debug.modelPickers() lists every picker mount; all read from /v1/models

- id: ux-polish-pass-dashboard
  subject: UX polish — cmd+k focus chat, localStorage persist model, sampler health pill
  status: pending
  description: cmd+k binds to chat textarea focus; last-selected model persists across reloads via localStorage; topbar shows sampler health pill (green/red dot count from /api/models/sampler). All anentrypoint-design changes.
  effort: medium
  category: feature
  route_family: representation
  load: 0.6
  failure_modes: [representation/poor-ux]
  route_fit: examined
  authorization: weak_prior
  blocking: []
  blockedBy: [dynamic-model-picker-everywhere]
  acceptance:
    - cmd+k focuses chat textarea
    - localStorage persists last model across reload
    - topbar sampler health pill renders count from /api/models/sampler

- id: penguins-agent-research-cycle
  subject: Drive freddie agent to do real research+edit work on penguins repo end-to-end
  status: pending
  description: Pick king-penguin.html. Use freddie agent (via penguins/freddie-task.js + tool-using model like claude-cli/sonnet or openrouter/anthropic/claude-sonnet-4) to read species.json record, add a new "Recent research 2024-2025" subsection with 3 facts and citations, update facts.json with 3 new entries with category+species, ensure penguins/test.js still passes. Save trace to penguins/.freddie/trajectories/.
  effort: large
  category: feature
  route_family: execution
  load: 0.9
  failure_modes: [execution/agent-loop-error, state/test-regression, reasoning/hallucinated-fact]
  route_fit: examined
  authorization: weak_prior
  blocking: []
  blockedBy: []
  acceptance:
    - king-penguin.html has a new "Recent research" section
    - facts.json has 3 new entries with category and species set
    - cd c:/dev/penguins && node test.js exits 0
    - trace artifact in penguins/.freddie/trajectories/

- id: browser-witness-everything
  subject: Browser-witnessed test (Playwright via gm:browser) for dashboard pages
  status: pending
  description: Boot dashboard, open #/models, #/chat, #/sessions in playwright, snapshot window.__debug.*, drag-reorder fallback and confirm /api/config persists, send a chat message and assert SSE message arrives. Save screenshots to .gm/shots/.
  effort: medium
  category: infra
  route_family: observability
  load: 0.7
  failure_modes: [observability/no-witness]
  route_fit: examined
  authorization: weak_prior
  blocking: []
  blockedBy: [dashboard-models-page-polish, dynamic-model-picker-everywhere]
  acceptance:
    - .gm/shots/models-page.png exists with provider grid
    - .gm/shots/chat-after-send.png shows assistant response
    - window.__debug snapshot saved to .gm/browser-witness.json
