// Shared props for every ic_ds_* icon component: { size?: number, className?: string }.
// Pure type-only file in the TypeScript source (an `IconProps` interface with no
// runtime representation); nothing imports it at runtime, so it is intentionally
// left empty after the type-stripping conversion.
